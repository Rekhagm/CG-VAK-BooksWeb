@model IEnumerable<CoverType>


<div class="container-fluid">

    <div class="row mt-4 mb-2">
        <div class="col-md-6">
             <h3>
                   CoverType Dashboard
            </h3>
        </div>
        <div class="col-md-6 text-end">
            <a asp-area="" asp-controller="CoverType" asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Create CoverType</a>
        </div>
    </div>
        
</div>


<partial name="_Notification"/>


    <table class="table table-hover">
        <thead class="table-primary">
            <tr>
                <th>Sl.No</th>
                <th>Name</th>
                <th>Action</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var covertype in Model.Select((value,index)=>new {index,value}))
            {
              <tr>
                  <td>@(covertype.index+1)</td>
                <td>@covertype.value.Name</td>
                <td>
                    <a asp-controller="CoverType" asp-action="Edit" asp-route-Id="@covertype.value.Id" class="btn btn-sm btn-secondary"><i class="bi bi-pencil-square"></i></a>
                    <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@covertype.value.Id"   class="btn btn-sm btn-danger"><i class="bi bi-trash"></i></a>
                </td>
           </tr>
            }
           
        </tbody>
    </table>
